# Capture: the second decision

## Decisions

### The manifest is named per run

Rejected: keeping one fixed path
Because: two runs sharing a temp directory overwrite each other's snapshot
Evidence: plugins/kb/scripts/check-docs.sh
Supersedes: record r1.md ### The manifest lives at a fixed path
