{
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'for r in \"${CLAUDE_PLUGIN_ROOT}\" \"${PLUGIN_ROOT}\"; do h=\"$r/hooks/mark-unpublished-capture.sh\"; [ -x \"$h\" ] && exec \"$h\"; done; exit 0'"
          }
        ]
      }
    ]
  }
}
