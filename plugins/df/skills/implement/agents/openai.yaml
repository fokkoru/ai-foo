interface:
  display_name: "Implement"
  short_description: "Execute plans with bounded background work"
  default_prompt: "Use $df:implement to execute this technical plan with main-thread work, safe background overlap, and joined verification."
policy:
  allow_implicit_invocation: false
