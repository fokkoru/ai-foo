interface:
  display_name: "Implement"
  short_description: "Execute plans one phase at a time"
  default_prompt: "Use $df:implement to execute this technical plan one phase at a time in the main thread, with per-phase verification and independent review."
policy:
  allow_implicit_invocation: false
